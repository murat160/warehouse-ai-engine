# Murat AI Studio — FULL stack для VPS / GPU deploy.
#
# Streamlit Cloud НЕ использует этот файл (там requirements.txt — light preview).
# На VPS ставится этот файл и поднимается полноценный AI backend:
# real translation, MMS-TTS, Whisper ASR, speaker diarization, voice cloning.
#
# Все пакеты — permissive (Apache 2.0 / MIT / BSD).
# Веса моделей выбираются runtime через MURAT_AI_TRANSLATION_MODEL
# (Apache 2.0 по умолчанию = google/madlad400-3b-mt).

-r requirements.txt

# --- Backend архитектура (FastAPI + Postgres + httpx + OpenAI клиент) ---
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
httpx>=0.27,<1.0
openai>=1.30,<2.0
SQLAlchemy>=2.0,<3.0
psycopg[binary]>=3.1,<4.0

# --- Translation / ASR / TTS — тяжёлые ML ---
transformers==4.51.3
torch>=2.2.0
sentencepiece==0.2.0
sacremoses==0.1.1
openai-whisper==20250625

# --- Audio processing ---
soundfile>=0.12,<1.0
scipy==1.13.1
numpy>=1.26.0

# --- Video / download ---
yt-dlp>=2025.1.1
ffmpeg-python==0.2.0
langdetect==1.0.9
