# Murat AI Studio — Streamlit Cloud LIGHT preview.
#
# Сюда попадают ТОЛЬКО UI-зависимости, чтобы pip install в Streamlit Cloud
# проходил за 30 секунд. Все heavy ML deps (torch, transformers, scipy,
# whisper, sentencepiece) → requirements-full.txt (для VPS/GPU).
#
# rebuild-marker: 2026-06-07-v4 (render-video-device / all-in-one-iframe)
# Force Streamlit Cloud container rebuild.

streamlit==1.45.1
python-dotenv>=1.0,<2.0
requests>=2.31,<3.0
