# Streamlit Cloud preview requirements for Murat AI.
# Keep this file lightweight so the UI can deploy quickly.
# Full AI/VPS requirements live in requirements-full.txt.
# rebuild-marker: 2026-05-24-22:00 (claude-aligned-video-studio)

# --- Core ---
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0
python-dotenv>=1.0,<2.0

# --- API architecture ---
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0

# --- HTTP / cloud providers ---
httpx>=0.27,<1.0
openai>=1.30,<2.0

# --- Persistent storage ---
SQLAlchemy>=2.0,<3.0

# --- Streamlit preview UI ---
streamlit==1.45.1

# --- Lightweight helpers ---
langdetect==1.0.9
yt-dlp>=2025.1.1
ffmpeg-python==0.2.0

# --- Tests / dev ---
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
