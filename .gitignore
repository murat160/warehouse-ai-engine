# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual envs
.venv/
venv/
env/

# Environment / secrets
.env
.env.*
!.env.example
!.env.production.example

# IDE
.idea/
.vscode/
*.swp

# Audio / video artefacts
*.wav
*.mp3
*.mp4
*.mkv
*.srt
*.vtt
artifacts/
tmp/

# Models cache
.cache/
hf_cache/

# Local storage (user glossary + TM SQLite db)
data/
out/

# Custom Voice samples — NEVER commit user audio to this repository.
# Profiles live in SQLite, the audio files live on disk only.
data/custom_voices/
*.wav
*.mp3
*.m4a
*.ogg
*.flac
*.webm

# Publishing inbox — finished media destined for YouTube / TikTok / etc.
# Stays on local disk only; never commit user-generated media.
data/publishing_inbox/
